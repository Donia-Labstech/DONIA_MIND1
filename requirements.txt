# ═══════════════════════════════════════════════════════════
# القائمة المدمجة والمحدثة لضمان دعم العربية وتنسيق الـ PDF
# ═══════════════════════════════════════════════════════════

# ── دعم اللغة العربية والخطوط (حل مشكلة المربعات ووضوح النص) ──
arabic-reshaper>=3.0.0      # لإعادة تشكيل الحروف لتبدو متصلة
python-bidi>=0.4.2           # لمعالجة النصوص من اليمين إلى اليسار (RTL)

# ── الأساسيات وواجهة المستخدم ────────────────────────────────
streamlit>=1.32.0
python-dotenv>=1.0.0
requests>=2.31.0

# ── الذكاء الاصطناعي و Groq و OpenAI ─────────────────────────
groq>=0.4.0
openai>=1.0.0
langchain-groq>=0.1.3

# ── قواعد البيانات والربط السحابي (Firebase & Google) ────────
firebase-admin>=6.2.0
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=1.0.0

# ── معالجة البيانات والجداول والرسوم البيانية ────────────────
pandas>=2.0.0
openpyxl>=3.1.2
xlsxwriter>=3.1.0
xlrd>=2.0.0
plotly>=5.18.0

# ── معالجة الصور و الـ OCR ورموز الاستجابة السريعة ───────────
Pillow>=10.0.0
pytesseract>=0.3.10
qrcode[pil]>=7.4.2

# ── معالجة وتوليد الملفات (PDF & Word & LXML) ───────────────
reportlab>=4.0.0             # المكتبة الأساسية لتوليد PDF بجودة عالية
python-docx>=1.1.0
lxml>=4.9.0
pdfplumber>=0.10.0           # لاستخراج النصوص من PDF بدقة
PyPDF2>=3.0.0
