# الأساسيات وواجهة المستخدم
streamlit>=1.32.0
python-dotenv>=1.0.0
requests>=2.31.0

# الذكاء الاصطناعي و Groq و OpenAI
groq>=0.4.0
openai>=1.0.0
langchain-groq>=0.1.3

# قواعد البيانات والربط السحابي (Firebase & Google)
firebase-admin>=6.2.0
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=1.0.0

# معالجة البيانات والجداول والرسوم البيانية
pandas>=2.0.0
openpyxl>=3.1.2
xlsxwriter>=3.1.0
xlrd>=2.0.0
plotly>=5.18.0

# معالجة الصور و الـ OCR ورموز الاستجابة السريعة
Pillow>=10.0.0
pytesseract>=0.3.10
qrcode[pil]>=7.4.2

# معالجة وتوليد الملفات (PDF & Word & LXML)
reportlab>=4.0.0
python-docx>=1.1.0
lxml>=4.9.0
pdfplumber>=0.10.0
PyPDF2>=3.0.0

# دعم اللغة العربية والخطوط
arabic-reshaper>=3.0.0
python-bidi>=0.4.2
from fpdf import FPDF

pdf = FPDF()
# يجب تحميل خط يدعم العربية (مثل Arial أو DejaVu)
pdf.add_font('Amiri', '', 'Amiri-Regular.ttf', uni=True) 
pdf.set_font('Amiri', size=14)

# إجبار النص على الاتجاه من اليمين لليسار
pdf.write(10, "النص العربي سيظهر هنا بشكل صحيح")

