# ═══════════════════════════════════════════════════════════
# DONIA MIND 1 — v3.0 Global Excellence Upgrade (Merged)
# ═══════════════════════════════════════════════════════════

# ── Core Framework (الأساسيات وواجهة المستخدم) ──────────────
streamlit>=1.35.0
python-dotenv>=1.0.0
requests>=2.31.0

# ── LLM Engines & AI (الذكاء الاصطناعي) ─────────────────────
langchain>=0.2.0
langchain-groq>=0.1.6
groq>=0.9.0
openai>=1.30.0

# ── Cloud & Databases (قواعد البيانات والربط السحابي) ────────
firebase-admin>=6.2.0
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=1.0.0

# ── Data Processing & Viz (معالجة البيانات والرسوم) ─────────
pandas>=2.0.0
numpy>=1.26.0
openpyxl>=3.1.2
xlsxwriter>=3.1.0
xlrd>=2.0.1
plotly>=5.18.0

# ── Image, OCR & QR (الصور ورموز الاستجابة) ────────────────
Pillow>=10.0.0
pytesseract>=0.3.10
qrcode[pil]>=7.4.2

# ── Document Handling (معالجة وتوليد الملفات) ───────────────
reportlab>=4.2.0
python-docx>=1.1.0
lxml>=5.0.0
pdfplumber>=0.10.0
PyPDF2>=3.0.0

# ── Arabic Rendering (دعم اللغة العربية والخطوط) ─────────────
arabic-reshaper>=3.0.0
python-bidi>=0.4.2
