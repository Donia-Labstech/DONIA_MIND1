# ============================================================
#              ملف متطلبات المشروع - requirements.txt
#        مُنظَّم ومُحدَّث بدعم كامل للعربية في PDF و LaTeX
# ============================================================


# ──────────────────────────────────────────────
# 1. الأساسيات وواجهة المستخدم
# ──────────────────────────────────────────────
streamlit>=1.32.0
python-dotenv>=1.0.0
requests>=2.31.0                # مطلوب أيضاً لتنزيل الخطوط العربية تلقائياً


# ──────────────────────────────────────────────
# 2. الذكاء الاصطناعي — Groq & OpenAI & LangChain
# ──────────────────────────────────────────────
groq>=0.4.0
openai>=1.0.0
langchain-groq>=0.1.3


# ──────────────────────────────────────────────
# 3. قواعد البيانات والربط السحابي (Firebase & Google)
# ──────────────────────────────────────────────
firebase-admin>=6.2.0
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=1.0.0


# ──────────────────────────────────────────────
# 4. معالجة البيانات والجداول والرسوم البيانية
# ──────────────────────────────────────────────
pandas>=2.0.0
openpyxl>=3.1.2
xlsxwriter>=3.1.0
xlrd>=2.0.0
plotly>=5.18.0
matplotlib>=3.7.0               # ضروري لرسم صيغ LaTeX كصور عند الحاجة


# ──────────────────────────────────────────────
# 5. معالجة الصور والـ OCR ورموز الاستجابة السريعة
# ──────────────────────────────────────────────
Pillow>=10.0.0
pytesseract>=0.3.10
qrcode[pil]>=7.4.2


# ──────────────────────────────────────────────
# 6. دعم اللغة العربية والخطوط — ★ قسم حرج ★
# ──────────────────────────────────────────────

# إعادة تشكيل الحروف العربية وضبط اتجاه النص (RTL)
arabic-reshaper>=3.0.0
python-bidi>=0.4.2

# أدوات الخطوط — تحميل وتضمين وتحويل ملفات .ttf/.otf
fonttools>=4.43.0               # تضمين الخطوط داخل PDF
brotli>=1.1.0                   # ضغط الخطوط داخل PDF/WOFF2 (تبعية fonttools)

# تحميل الخطوط العربية تلقائياً عند الحاجة
# ← البرنامج يستخدم `requests` لتنزيل خط Amiri أو Cairo من Google Fonts
# ← راجع ملف src/utils/font_loader.py للتفاصيل


# ──────────────────────────────────────────────
# 7. توليد ومعالجة ملفات PDF — مع دعم العربية الكامل
# ──────────────────────────────────────────────

# ReportLab — إنشاء PDF (يدعم العربية بعد تسجيل خط TTF عربي)
reportlab>=4.0.0

# FPDF2 — بديل أخف يدعم Unicode والعربية مباشرةً بدون إضافات
fpdf2>=2.7.9

# قراءة PDF واستخراج النصوص
pdfplumber>=0.10.0
PyPDF2>=3.0.0

# تحويل HTML إلى PDF مع دعم RTL والخطوط العربية (عبر WeasyPrint)
weasyprint>=62.0                # يستخدم Pango/Cairo ويدعم العربية تلقائياً
# ملاحظة: يحتاج تثبيت مكتبات النظام:
#   Linux:   sudo apt-get install libpango-1.0-0 libharfbuzz0b libpangoft2-1.0-0
#   macOS:   brew install pango
#   Windows: راجع https://doc.courtbouillon.org/weasyprint/stable/first_steps.html


# ──────────────────────────────────────────────
# 8. دعم LaTeX — عرض الرموز بشكل صحيح
# ──────────────────────────────────────────────

# توليد مستندات LaTeX برمجياً
pylatex>=1.4.2

# تحويل صيغ LaTeX إلى صور PNG/SVG (بدون تثبيت LaTeX على النظام)
latex2mathml>=3.77.0            # تحويل LaTeX → MathML (للعرض في HTML)
sympy>=1.12                     # رياضيات رمزية + تصدير LaTeX
# matplotlib يُغطي رسم المعادلات كصور (مُدرج في القسم 4)

# ملاحظة: لعرض LaTeX كاملاً داخل PDF استخدم:
#   - matplotlib مع fig.savefig() لتحويل المعادلة إلى صورة ثم تضمينها
#   - أو weasyprint مع MathJax عبر HTML


# ──────────────────────────────────────────────
# 9. معالجة وتوليد ملفات Word و XML
# ──────────────────────────────────────────────
python-docx>=1.1.0
lxml>=4.9.0


# ──────────────────────────────────────────────
# 10. أدوات مساعدة إضافية
# ──────────────────────────────────────────────
pathlib2>=2.3.7; python_version < "3.10"   # توافق المسارات
urllib3>=2.0.0                              # مطلوب من requests

# ============================================================
# تعليمات تنزيل الخطوط العربية تلقائياً:
#
#   ضع هذا الكود في بداية تطبيقك (مثلاً app.py أو font_loader.py):
#
#   import requests, os
#   FONT_URL = "https://github.com/google/fonts/raw/main/ofl/amiri/Amiri-Regular.ttf"
#   FONT_PATH = "fonts/Amiri-Regular.ttf"
#   if not os.path.exists(FONT_PATH):
#       os.makedirs("fonts", exist_ok=True)
#       r = requests.get(FONT_URL); open(FONT_PATH, "wb").write(r.content)
#
#   ثم سجّل الخط في ReportLab:
#   from reportlab.pdfbase import pdfmetrics
#   from reportlab.pdfbase.ttfonts import TTFont
#   pdfmetrics.registerFont(TTFont("Amiri", FONT_PATH))
#
#   أو في FPDF2:
#   pdf = FPDF(); pdf.add_font("Amiri", fname=FONT_PATH)
# ============================================================
